# OS / editor
.DS_Store
.idea/
.vscode/

# Environment secrets
.env
.env.local
.env.*.local

# Agent (Node)
agent/node_modules/
agent/dist/

# Web (Next.js)
web/node_modules/
web/.next/
web/out/
web/.vercel/
web/*.tsbuildinfo

# Contract (Rust / Odra)
contract/target/
contract/wasm/
contract/Cargo.lock
contract/.env
contract/.backend*
contract/.builder*

# Keys (never commit)
*.pem
